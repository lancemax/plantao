<h1>Listing packages</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Amount</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @packages.each do |package| %>
  <tr>
    <td><%= package.name %></td>
    <td><%= package.price %></td>
    <td><%= package.amount %></td>
    <td><%= link_to 'Show', admin_package_path(package) %></td>
    <td><%= link_to 'Edit', edit_admin_package_path(package) %></td>
    <td><%= link_to 'Destroy', admin_package_path(package), confirm: 'Você tem Certeza de que quer deletar?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package', new_admin_package_path %>
