<%= form_for(@push_time) do |f| %>
  <% if @push_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@push_time.errors.count, "error") %> prohibited this push_time from being saved:</h2>

      <ul>
      <% @push_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
